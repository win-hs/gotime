# GoTime 開發進度

> **接手方式**：讀本檔 →「下一步」段落即為待辦。
> 完整規格在 `E:\9_ClaudeSpace\CATSpec\PLAN-gotime.md`（不在本 repo，含 §10 氣象署 API 實測紀錄）。
> 本機預覽：`preview_start` 用設定名 **gotime**（port **4325**，非規格原寫的 4323，該埠已被 batch-triangulation 佔用）。

最後更新：2026-08-01

---

## 已完成

### 階段 A — 資料源實測 ✅
- 氣象署 API 以真金鑰實測通過，**CORS `Access-Control-Allow-Origin: *`，瀏覽器可直連**，不需 Apps Script 代理。
- 產出 `data/towns.json`（368 鄉鎮，含 `ds3`/`ds7` 對應的縣市資料集代碼）與
  `data/tide-points.json`（266 潮汐點，含 `kind` 類型與座標）。
- 三個陷阱記於規格 §10：全臺天氣代碼 `-089`/`-091` 只有縣市層級（鄉鎮須用 22 個縣市專屬碼）、
  潮汐 `Daily` 陣列未依日期排序、8 個重名鄉鎮須用 `Geocode` 區分。

### 階段 B — 骨架＋天文計算 ✅
- `index.html`／`css/app.css`：卡片式版面、深淺主題、手機單欄（實測 375px 無橫向溢出）。
- 地圖選點（Leaflet 本地檔，複製自 CATPatrol）、圖釘可拖曳、座標貼上、瀏覽器定位。
- 日期列：預設今天、前後一天、任選日期；URL 參數 `?lat&lon&date` 可還原畫面。
- 太陽卡：日出日落、三段曙暮光（民用/航海/天文）、日中、日長。
- 月亮卡：月相名稱、照度%、盈虧、月出月落、SVG 月相圖（盈相亮面在右、虧相在左）。

**`js/moonpos.js` 是本階段的重點**：SunCalc 的月球黃經只取主要中心差一項，
略去出差/二均差，誤差達 1°（≈4 分鐘升落時刻），實測與氣象署最大差 **10 分鐘**。
改以 Meeus 第 47 章主要週期項（表 47.A/47.B 各 60 項）自行計算，
月出月落判準用「上緣接觸地平線」＝ `−(0.5667° + 0.2725 × 地平視差)`，並做周日視差修正。

#### 驗證結果（對氣象署官方資料）
| 項目 | 樣本 | 結果 |
|---|---|---|
| 日出/日落/民用曙暮光 | 花蓮 2026-08-01 | 差 **≤1 分鐘** |
| 月出/月落 | 花蓮 2026-08-01～31（29 天） | 平均 −0.5 分、**最大 1 分鐘** |

比對基準：`A-B0062-001`（日出日沒）、`A-B0063-001`（月出月沒），
座標取花蓮縣政府 `23.9911, 121.6014`（實測與氣象署「花蓮縣」代表點吻合）。

### 階段 C — 氣象署串接 ✅
- **`js/cwa.js`**（唯一碰氣象署的模組）：帶金鑰、15 秒 timeout、錯誤訊息中文化、記憶體快取；
  上層只看正規化結構。潮汐帶 `LocationName`；天氣帶 `LocationName`＋`ElementName`。
  天氣以 `ds7`（1 週逐 12 小時）為主，**僅當所選日期早於 ds7 起始日**才退回 `ds3`（逐 3 小時，涵蓋當天剩餘時段）——
  晚於結束日代表兩者都沒有，不再多打一次請求。
- **`js/geo.js`**：haversine 最近鄉鎮／最近潮汐點（含距離）、Nominatim 地名搜尋（搜尋框同時吃地名與座標）。
- **潮汐卡**：滿乾潮時刻＋潮高（`AboveChartDatum`）、大/中/小潮徽章、農曆日期、最近潮汐點與距離。
- **整月潮汐表**：覆蓋層表格，**已依 `Date` 排序**（API 回傳是亂序的），當日高亮。
- **天氣卡**：逐時段天氣現象、溫度區間、降雨機率、風級、紫外線（要素段數不同，**以 `StartTime` 對齊合併**）。
- 卡片級錯誤處理：各卡獨立 loading／錯誤＋重試鈕；`reqSeq` 防止快速切換時舊回應覆蓋新結果。

#### 驗證結果
| 情境 | 結果 |
|---|---|
| 沿海（苗栗竹南 24.7002,120.8461） | 潮汐點取到「漁港龍鳳」距 1.1 km；天氣「竹南鎮」距 2.5 km，逐 12 小時 4 筆潮汐正常 |
| 內陸（南投埔里 23.9650,120.9650） | 潮汐標示最近點「海釣臺中港100號碼頭」距 **56.6 km**；天氣「埔里鎮」距 0.5 km |
| 超出天氣預報範圍（2026-08-25） | 天氣卡顯示「此日期超出預報範圍　可預報 …」，潮汐與天文卡照常 |
| 整月潮汐表 | 32 列、**日期排序正確**、當日高亮、大/中/小徽章齊全 |
| 手機 375px | 無橫向溢出，4 張卡單欄，覆蓋層 343px 完整顯示 |

主控台無錯誤。

---

## 下一步：階段 D — 規劃／常用／分享／匯出

1. **`js/plan.js` 規則引擎**：規則＝名稱＋起訖兩個「錨點＋偏移分鐘」。
   錨點：日出、日落、三段曙暮光各始終、月出、月落、**滿潮、乾潮**、固定時刻。
   - 潮汐錨點要對當日各潮次逐一展開（一天多次滿乾潮 → 產生多個時段）。
   - 該地無潮汐資料時，該規則顯示「此地點無潮汐資料」。
   - 內建範例：「鳥類調查：民用曙光始 −30 分 ～ 日出 +180 分」「潮間帶調查：乾潮 −120 分 ～ 乾潮 +120 分」。
2. **`.ics` 匯出**：勾選時段 → VEVENT（標題＝規則名＋地點、描述附當日日出日落/月相/滿乾潮摘要），Blob 下載。
   ⚠ **必須實測 Google 日曆匯入時間不偏移**（用 `TZID=Asia/Taipei` 或 UTC 換算擇一）。
3. **常用地點**：localStorage 存清單，頂欄下拉快速切換；localStorage 不可用時停用該功能但不影響查詢。
4. **分享按鈕**：組 `?lat&lon&date` 複製到剪貼簿（URL 參數讀寫已完成，只差按鈕）。

## 之後：階段 E
push `win-hs/gotime` → Pages → OAuth 無關（本專案不需登入）→ 手機實測全流程。

---

## 待確認事項
- 海況／波浪預報第一版不做（使用者未回覆，預設排除）。
- 分享按鈕與常用地點的 UI 尚未加入頁面（階段 D）。
- `index.html` 目前只有太陽、月亮兩張卡，潮汐/天氣/規劃卡待階段 C、D 加入。
